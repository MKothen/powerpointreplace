rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /lecternDecks/{deckId} {
      allow read, write: if request.auth != null;

      match /slides/{slideId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
